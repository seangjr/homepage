import { Box } from "@chakra-ui/react";
import styled from "@emotion/styled";

export const QualificationSection = styled(Box)`
  padding-left: 3.4em;
  text-indent: -3.4em;
`;

export const QualificationYear = styled.span`
  font-weight: bold;
  margin-right: 1em;
`;
